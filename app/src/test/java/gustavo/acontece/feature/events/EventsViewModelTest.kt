package gustavo.acontece.feature.events

class EventsViewModelTest {

    fun `when fetch data with success must set into list`() {

    }

    fun `when fetch data with error must show error info`() {

    }
}